# oc4ids-datastore-frontend

## Developing or deploying

You need to copy the file `js/endpoint.js.example` to `js/endpoint.js` 
then edit `js/endpoint.js` (If the default values are not acceptable)

